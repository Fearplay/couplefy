class NameDayModel {
  NameDayModel(this.name, this.date, this.languageCode);

  //language code just in case if I would make also American calendar
  final String name;
  final DateTime date;
  final String languageCode;
}
